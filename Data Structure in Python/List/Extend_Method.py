#The extend() method in Python is used to add all elements of another iterable to the end of a list.

a = [1, 2, 3]
b = [4, 5]

a.extend(b)
print(a)

# extend() adds elements one by one . It modifies the original list